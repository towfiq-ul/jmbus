/**
 * @author Towfiqul Islam
 * @since 2/5/23 4:29 PM
 */

package io.github.towfiqul.jmbus;

/**
 * Placeholder entry point class, referenced as the jar manifest's {@code Main-Class} by the
 * build (see {@code pkg.mainClass} in {@code pom.xml} / the {@code jar} block in
 * {@code build.gradle}). {@code jmbus} is used as a library via {@link
 * io.github.towfiqul.jmbus.service.MBusService}, not run directly, so this class intentionally
 * has no behavior.
 */
public class JMBusApplication {

    /** No-arg constructor; this class has no state or behavior. */
    public JMBusApplication() {
    }
}
